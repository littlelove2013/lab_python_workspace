import numpy as np
xr=100#x行数
yr=1000#y的行数
c=100#特征列数
#模拟输入数据
X=np.random.rand(xr,c)
Y=np.random.rand(yr,c)
def knn():
    if(len(X[0])!=len(Y[0]))
    Xdouble=np.diag(X.dot(X.T))
    Ydouble=np.diag(Y.dot(Y.T))
    return 0
if __name__ == '__main__':
    knn()